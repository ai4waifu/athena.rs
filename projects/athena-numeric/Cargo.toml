[package]
name = "athena-numeric"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Athena numeric tower — values, domains, precision, promotion, certificates"
publish = false

[features]
default = ["portable"]
portable = []
native-accelerated = []
mkl = ["native-accelerated"]
blas = ["native-accelerated"]
# Batch-local `TemporaryNatural` / `TemporaryInteger`（Living `31`；历史别名 `Ephemeral*`；供 athena-benchmark）。
ephemeral = []

[[test]]
name = "ephemeral_batch"
path = "tests/ephemeral_batch.rs"
required-features = ["ephemeral"]

[dependencies]
athena-types = { workspace = true }
athena-gc = { workspace = true }

[lints.rust]
missing_docs = "warn"
# Magnitude union / OwnedLimbBuffer 需要窄域 unsafe；其余模块保持 deny。
unsafe_code = "deny"
